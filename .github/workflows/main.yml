name: Carbon Test Workflow

on:
  push:
  workflow_dispatch:

jobs:
  short-test:
    name: Short Test (10 seconds)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Run quick test
        run: |
          echo "🚀 Starting quick test..."
          sleep 10
          echo "✅ Quick test complete!"

  medium-test:
    name: Medium Test (15 seconds)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Run medium test
        run: |
          echo "🚀 Starting medium test..."
          sleep 15
          echo "✅ Medium test complete!"

  long-test:
    name: Long Test (20 seconds)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Run long test
        run: |
          echo "🚀 Starting long test with Node.js..."
          sleep 20
          echo "✅ Long test complete!"
